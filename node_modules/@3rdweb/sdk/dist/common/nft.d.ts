import { LazyNFT, NFT, NFTCollection } from "@3rdweb/contracts";
import { JSONValue, ProviderOrSigner } from "../core/types";
/**
 * The shared NFT metadata.
 * @public
 */
export interface NFTMetadata {
    id: string;
    uri: string;
    name?: string;
    description?: string;
    image?: string;
    properties?: Record<string, JSONValue>;
}
/**
 * The shared NFT metadata, including the current owner address.
 * @public
 */
export interface NFTMetadataOwner {
    owner: string;
    metadata: NFTMetadata;
}
/**
 * @internal
 */
export declare type NFTContractTypes = NFT | NFTCollection | LazyNFT;
/**
/* @internal
 */
export declare function getMetadataWithoutContract(provider: ProviderOrSigner, contractAddress: string, tokenId: string, ipfsGatewayUrl: string): Promise<NFTMetadata>;
/**
/* @internal
 */
export declare function getTokenMetadata(contract: NFTContractTypes, tokenId: string, ipfsGatewayUrl: string): Promise<NFTMetadata>;
/**
/* @internal
 */
export declare function getTokenUri(contract: NFTContractTypes, tokenId: string): Promise<string>;
